export class Categories {
    public static categories : Array<Category> = [
        {
            _id: "string",
            name: "string"
        }
    ];
}


export default interface Category{
    _id: string,
    name: string
}

